---
globs: client/**/*
alwaysApply: false
---

# Frontend context (auto-applied on `client/**`)

When editing frontend code, the architecture, conventions, and validation rules below always apply. The full feature / bugfix / refactor workflow (context loading, plan, branch, PR, validation, push) lives in the [implement-client](../skills/implement-client/SKILL.md) skill — invoke it explicitly when starting a task.

## Reference reads (load on demand)

- [docs/frontend/architecture.mermaid](../../docs/frontend/architecture.mermaid) — module boundaries, component relationships.
- [docs/frontend/architecture.md](../../docs/frontend/architecture.md) — descriptive architecture, guidelines, patterns.
- [docs/frontend/technical.md](../../docs/frontend/technical.md) — technical specifications.

These are read by the [implement-client](../skills/implement-client/SKILL.md) skill at the start of a task. For ad-hoc edits, load them only when the change is non-trivial.

## Workflow

For the full end-to-end feature workflow (context load → plan → branch → implement → docs sync → review → push), use the [implement-client](../skills/implement-client/SKILL.md) skill.
