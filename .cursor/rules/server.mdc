---
alwaysApply: false
---
# Backend context (auto-applied on `server/**`)

When editing backend code, the architecture, conventions, and validation rules below always apply. The full feature / bugfix / refactor workflow (context loading, plan, branch, PR, validation, push, Postman + migration updates) lives in the [implement-server](../skills/implement-server/SKILL.md) skill — invoke it explicitly when starting a task.

## Reference reads (load on demand)

- [docs/backend/architecture.mermaid](../../docs/backend/architecture.mermaid) — module boundaries, data flow, dependencies.
- [docs/backend/architecture.md](../../docs/backend/architecture.md) — descriptive architecture, guidelines, patterns.
- [docs/backend/technical.md](../../docs/backend/technical.md) — technical specifications.

These are read by the [implement-server](../skills/implement-server/SKILL.md) skill at the start of a task. For ad-hoc edits, load them only when the change is non-trivial.

## Workflow

For the full end-to-end feature workflow (context load → plan → branch → implement → contract tests → Postman / migration updates → docs sync → review → push), use the [implement-server](../skills/implement-server/SKILL.md) skill.
